# Ansible Vault — production secrets for grilling-sleek.
#
# Currently no application secrets needed (SQLite has no password, no JWT, no API keys).
# This file is encrypted with ansible-vault. Decrypt with:
#   ansible-vault view devops/ansible/vars/production/vault.yml --vault-password-file ~/.ansible-vault/grilling-sleek-production.pwd
#
# Placeholder for future secrets:
# some_secret: changeme
