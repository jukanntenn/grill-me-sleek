all:
  children:
    # fn — 内网 NAS，staging 环境：滚动 main tag（本机 build.py --push 到
    # 私有 registry 192.168.5.50:5000），HTTP 直连，坏了可随时重推。
    staging:
      hosts:
        fn:
          ansible_host: 192.168.5.200
          ansible_user: fn
    # ttyo — VPS，production 环境：Cloudflare 回源（Origin CA + CIDR 限源），
    # 镜像 pin Docker Hub 正式版 tag。
    production:
      hosts:
        ttyo:
          ansible_host: 43.133.160.29
          ansible_user: alice
