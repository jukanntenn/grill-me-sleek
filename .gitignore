# Python (scripts still present: sync-version.py, build.py, publish-local.py)
__pycache__/
*.py[cod]

# Environments
.env

# OS
.DS_Store
Thumbs.db

# Editors
*.swp
*.swo
*~
.idea/
# Share VSCode tasks/launch/extensions with the team; only ignore personal
# workspace settings.
.vscode/settings.json

.local/

# Rust
server/target/
server/data/
server/log/
server/.vite/

# Node
node_modules/
# Per-package lockfiles only; no workspace root install — a root
# pnpm-lock.yaml is an accidental `pnpm install` at the repo root.
/pnpm-lock.yaml
web/dist/
cli/dist/
dsh/dist/
dsh/*.tgz

# Playwright test artifacts (screenshots/snapshots regenerated on test runs)
e2e/playwright-report/
e2e/test-results/

# ZCode session plans
.zcode/plans/
.playwright-cli/
