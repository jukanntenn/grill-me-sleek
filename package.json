{
  "name": "grill-me-sleek",
  "version": "0.2.0-rc.1",
  "private": true,
  "description": "Stress-test your plan before vibe coding",
  "scripts": {
    "version": "python3 scripts/sync-version.py && git add cli/package.json web/package.json server/Cargo.toml",
    "postversion": "git push && git push --tags"
  }
}
