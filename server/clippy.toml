# Clippy configuration.
#
# `msrv` MUST be a quoted string (TOML would parse an unquoted 1.85 as a float
# and clippy's deserializer rejects it). Keep in sync with `Cargo.toml`'s
# `rust-version`.

msrv = "1.85"
